import React from 'react';
import { Grid } from '@mui/material';

const ShowProduct = () => (
  <Grid container justifyContent='center'>
    <Grid item>Show Product</Grid>
  </Grid>
);

export default ShowProduct;
